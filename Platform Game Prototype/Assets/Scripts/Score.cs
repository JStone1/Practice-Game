using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Score : MonoBehaviour
{

    public int groundScore;
    // Start is called before the first frame update
    void Start()
    {
        groundScore = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
